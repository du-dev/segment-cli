# =============================================================================
# Dépendances du projet segment-cli
# =============================================================================
# Application de segmentation de clients via K-Means avec interface Streamlit.
# Toutes les versions sont exactes pour garantir la reproductibilité.
# =============================================================================

# Interface web interactive pour l'application de segmentation
streamlit

# Manipulation et analyse des données tabulaires (DataFrames)
pandas

# Calculs numériques et opérations sur les tableaux
numpy

# Algorithmes de machine learning (K-Means, normalisation, métriques)
scikit-learn

# Graphiques interactifs (scatter plots 3D, visualisation des clusters)
plotly

# Outils scientifiques supplémentaires (statistiques, optimisation)
scipy
